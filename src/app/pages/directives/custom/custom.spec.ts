import { CustomColor } from './custom';

describe('CustomColor', () => {
  it('should create an instance', () => {
    const directive = new CustomColor();
    expect(directive).toBeTruthy();
  });
});
